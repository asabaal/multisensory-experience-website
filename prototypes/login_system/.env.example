# Asabaal Ventures - Worker Daemon Environment Configuration
# Copy this to .env and fill in your actual values

# Supabase Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Worker Configuration
WORKER_ID=local-1
LEASE_SECONDS=300
POLL_INTERVAL=3

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=/var/log/asabaal-worker.log

# Processing Configuration
TEMP_DIR=/tmp/asabaal_jobs
MAX_FILE_SIZE_MB=500

# Optional: FFmpeg Path (if not in PATH)
FFMPEG_PATH=/usr/bin/ffmpeg

# Optional: GPU Configuration (for future use)
CUDA_VISIBLE_DEVICES=0

# Instructions:
# 1. Copy this file to .env
# 2. Fill in your Supabase project URL and service role key
# 3. Adjust worker settings as needed
# 4. Make sure the temp directory exists and is writable
# 5. Install dependencies: pip install -r requirements.txt